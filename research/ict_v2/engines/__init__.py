"""Detector/state-machine engines (WP-B/C, not yet built): swings, structure,
displacement, levels, sweeps, zones, ranges, amd, opening_range, overnight.
Each will implement the `EngineProtocol` from `core/runner.py`. Empty in WP-A.
"""
