"""ICT V2 — Phase 2 causal event engine (research/ict_v2).

Engineering only: every ICT concept as a versioned, lifecycle-managed,
prefix-invariant event stream. NO edge claims -- no PF, win rate, expectancy,
or returns anywhere in this package, its tests, or its reports. Semantic
certification is the only output.

Binding contract: `research/ict_v2/SPEC.md`. Work packages:
  WP-A (this + core/*)  -- foundations: events, clock, config, prefix, runner, gated stubs
  WP-B                  -- detectors: swings, structure, displacement, levels, opening_range, overnight
  WP-C                  -- state machines/zones: sweeps, zones, ranges, amd
  WP-D                  -- certification: parity/model01_canary.py + full-suite green + report
"""
