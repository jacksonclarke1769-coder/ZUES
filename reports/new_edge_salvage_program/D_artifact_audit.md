# Salvage Program — Artifact Audit (2026-07-06)
- Canaries: every lane reproduced its pinned references exactly (honest streams 583/705; the
  (10,$1200) row; VPC 408-signature; funded generalization vs apex_funded_40; A5 exit3 re-walk
  +89.2R from re-derived raw params). Look-ahead/timestamp canaries green in the suite (851).
- PF>1.8 freeze: zero triggers across ~700 computed cells program-wide (range 0.34-1.44).
- Denominator rule: count columns everywhere; no artifact found.
- One documented measurement subtlety (A5): bit-exact exit3 reproduction requires the model's
  stored tick-rounded target, not naive entry+2R — flagged, not patched.
- Honest disclosure: dl/tl-frequency and same-day-corr column definitions in the VPC/portfolio
  lanes were implementer-chosen mechanical definitions (documented in-file) — treat as
  descriptive, not certified metrics.
- Multiple-comparisons: B7's 3 marginal rules over 132 cells explicitly framed as noise-grade.
- Windows: combined-portfolio rows are 2022-2026 (VPC stream start) — stated in every table.
