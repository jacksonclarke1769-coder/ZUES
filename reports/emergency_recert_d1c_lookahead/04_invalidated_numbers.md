# D1c Attachment Timestamp Look-Ahead — Invalidation Ledger

INC-20260706-1141. This is a ledger only — no files below are edited by this task. Regeneration
happens in a later, separate lane once `run_d1c_real.attach_drift` is fixed and re-certified.
Mechanism/consumer-graph detail: `01_defect_trace.md` in this directory.

## INVALID-UNTIL-REGENERATED

| # | Number / artifact | Where it lives |
|---|---|---|
| 1 | **47.8% pass / 15.9% bust / 36.2% expire / median 16d** row (cap-10 re-lock) | `reports/apex_validation.json` key `cap10_relock_2026-07-05` (`eval.pass_pct=47.8`, `bust_pct=15.9`, `expire_pct=36.2`, `median_days=16`); `AGENTS.md:14,20`; `README.md:73-75,82,99`; `MONDAY_LIVE_RUNBOOK.md:7`; `OPERATOR_RUNBOOK.md:3`; `zeus_server.py:224,877,922` (hardcoded constants `pass_pct=47.8, bust_pct=15.9, expire_pct=36.2, median_days=16` / `certified_pass=47.8`); vault machine page `12 Knowledge Base/Production Machine/ZEUS Production Machine v2026.07.02b.md` (already banner-locked by vault commit b4e66c4 per `00_incident_preflight.md`) |
| 2 | **Per-year pass-rate table** (2021 51.1/45, 2022 38.6/70, 2023 52.8/89, 2024 35.1/74, 2025 62.9/89, 2026 35.7/28, All 47.8/395) | `README.md:89-99` |
| 3 | **WR 58.6% benchmark** ("per-trade WR ~58.6% at the certified config") | vault `12 Knowledge Base/Strategy/Profile A (ICT OTE).md:47` |
| 4 | **PF 2.31 kept-set** (D1c-filtered stream PF, "distrust-grade gap" vs unfiltered 1.24) | vault Profile A page (same line as WR 58.6, `Profile A (ICT OTE).md:47`); quoted in Fable briefing 1.3 per the incident note |
| 5 | **cap-15 x $1,000 SIM CONDITIONAL candidate** | vault decision `08 Decisions/DEC-20260705-2101-cap15-remains-sim-conditional-kill-line-15pct.md` — gate is now moot pending re-certification (built on the same defective stream) |
| 6 | **Funded funnel: ~$12.6-12.8k lifetime/PA, ~0% bust at A4-A5** | `reports/funded_funnel_2026-07-05.json` (`canary.A4.certified=[12649, 0.0, 16.1]`, `canary.A5.certified=[12827, 0.0, 15.9]`); `apex_funded_40.py` (imports `a_streams_d1c` directly, `apex_funded_40.py:28-31`); README.md:105-106 references this harness |
| 7 | **Conditional pass forecaster cache** (`reports/eval_day_pnl_50k_1200.json`) + its "~20% conditional read" | file itself (generated by `tools_eval_forecast.py:_rebuild_cache`, line 44, `a_streams_d1c` call); consumed live by `eval_forecast.py` (`CACHE_PATH`, line 26) for the operator's in-flight P(pass) readout |
| 8 | **Pass-path anatomy numbers** (breakdown of pass/bust/expire mechanics across cap x budget cells) | `reports/eval_sizing_sweep_2026-07-05.json` (`matrix`/`buckets`/`overlays` keys), produced by `tools_eval_sizing_sweep.py` (imports `a_streams_d1c`, line 36) |
| 9 | **Weekly-cadence replay numbers** (1G recycle-rule funnel, 1H dead-clock start-timing) | `tools_sprint_cadence.py` outputs — built on `tools_sim_parity_check.load_rows()`/`group_by_day` (line 60), which is `a_streams_d1c`-derived |

All nine items share one root cause: they are all downstream, by direct or transitive import, of
`tools_phase3_config_sweep.a_streams_d1c` (`~/trading-team/bot/nq-liq-bot/tools_phase3_config_sweep.py:35-62`),
which calls the defective `run_d1c_real.attach_drift` (`~/trading-team/backtests/ict-nq-framework/run_d1c_real.py:42-64`).
See `01_defect_trace.md` §7 for the full per-file import chain.

## SURVIVES

| Item | Why it survives |
|---|---|
| **Live execution stack** (all guardians/telemetry/watchdog: `flatten_guardian.py`, `deadman_watch.py`, `exec_telemetry.py`, `heimdall_monitor.py`, `feed_watch.py`, etc.) | Never consumed the research D1c stream; operates on live broker/feed state only. |
| **Unfiltered Profile A honest edge — PF 1.237 @ 1m truth** | Produced by `tools_1m_truth_recert.py`'s `a_streams()` (no `d1c`, no `attach_drift` call) — independent of the D1c attachment entirely; corroborated by the incident's own honest-stream re-derivation (contaminated 2.31 vs honest 1.36 on the KEPT subset; the unfiltered PF 1.237/1.21 figure referenced on the vault Profile A page is a separate, non-D1c-gated statistic). |
| **Exit#3 mechanics** | The partial-then-runner exit logic itself (`params["partial"]` in `model01_sweep_mss_fvg.py`, `strategy_engine_profileA.py`) is unaffected by the D1c join; it operates on the same `fill`/`exit_i` bar indices used everywhere. Re-evaluate its numbers on the honest (re-derived) stream once `attach_drift` is fixed — the mechanism itself needs no change. |
| **D1c live gate logic** (`drift_gate.py`, class `DriftGate`) | Causal by construction — fed `ts` live off completed 1m bars (`on_bar_close`) and the real 09:30 open (`on_session_open`), never through `reset_index()`/`.values`/string round-trip. CERBERUS-tested (2026-06-12, referenced `drift_gate.py:3`). This is the component that would actually trade live; it was never wrong. |
| **The DENOMINATOR-ARTIFACT method rule** (adopted 2026-07-06, `DEC-20260706-1108`) | A methodological rule about count-basis reporting for avoid/skip filters — orthogonal to which stream is used as input; still correct to apply once numbers are regenerated. |
| **Firewall / gate infrastructure** (`test_eval_config_firewall.py`, `test_funded_config_firewall.py`, `gate.sh`, `auto_safety.py`) | Config-hash and process-safety checks, not statistical claims about the D1c stream; confirmed still green (see below). |

## Firewall check (before/after this reporting task)

```
$ python3 -m pytest test_eval_config_firewall.py test_funded_config_firewall.py -q
5 passed in 0.09s
```
Trivially green both before and after, as expected — this task wrote only new files under
`reports/emergency_recert_d1c_lookahead/`; no code, config, or existing report was modified.
