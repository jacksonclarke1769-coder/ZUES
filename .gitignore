config.py
data/*.db
__pycache__/
*.pyc

# live-arming / gate flags — deliberate LOCAL action, never committed
evidence/approvals/*.flag
evidence/launchlock/**/*.flag
out/heimdall/
logs/

# research data cache (regenerable, large) + runtime P&L ledger (written by the live runner)
data/nightwatch/
out/ares/trade_results.csv

# local secrets — webhook URLs etc. (never commit)
.env
