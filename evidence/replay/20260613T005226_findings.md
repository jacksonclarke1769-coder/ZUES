# HERMES DRILL FINDINGS — 2026-06-13 (run 20260613T005226)

Drill verdict: PASS (all 9 acceptance criteria — see manifest). Rehearsal-only; nothing counts.

## Finding 1 — keep-rate runs hot on the bot pipeline (93.9% vs research 76.5% same window)
Decomposition: 62% validated-era baseline -> 76.5% Q1-2025 regime (research population, same
window) -> 93.9% bot pipeline. NOT a population difference (28/28 bot trade-days are research
trade-days; 96% same direction). Driver = decision-evaluation TIME: the shadow-on-logs
approximation evaluates at the bot's logged times; research evaluated at fill. 6/43 paired
decisions flip, ALL in the optimistic direction (bot-KEEP where research-DROP because drift
flipped between signal and fill).

## Finding 2 — implication for the live trial (NO action now, NO threshold changes)
The production gate is CONTINUOUS (suspend-while-disagree at every 1m close), which is
stricter than the shadow-on-logs approximation and resolves Finding 1 mechanically: a
pending order whose drift flips before fill will be suspended at fill time. Expectations:
- live keep-rate may run above 62% in strong-trend quarters (Q1-2025 research was 76.5%);
  the 45-80% heimdall band can legitimately kiss its upper edge in such regimes.
- If the live trial reports keep-rate >80%, ATHENA rules already say HOLD pending
  investigation — the FIRST check should be evaluation-time semantics (this finding),
  not edge drift.
- For B1 runner integration: the gate decision MUST be wired at order-management time
  (continuous), not at signal time — already the integration spec; this drill confirms why.

## Finding 3 — pipeline mechanics all green
24,246 bars streamed sequentially (no lookahead path exists: ReplayFeed yields in order);
33 signals; 33 isolated decisions; dual-implementation agreement 100%; fail_open 0;
official ATHENA state byte-identical before/after; replay rows force-ineligible even under
simulated eligibility override (tested).
